.column.portfolio-item(class="" data-bg=item.gallery[0].url)
	a(href='/portfolio/'+item.slug+'.html')
		.item
			.item-inner
				h6= item.title
				p.tags
					if item.tags
						for tag in item.tags.split(',')
							//- span.label= '#'+tag
							span.label= tag
